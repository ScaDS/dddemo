version: '3.8'

services:
  baby-drift:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./dataset:/app/dataset:ro
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
